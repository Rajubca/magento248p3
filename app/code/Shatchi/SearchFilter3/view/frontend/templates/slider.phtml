<?php
/** @var \Shatchi\SearchFilter3\Block\PriceSlider $block */
$objectManager = \Magento\Framework\App\ObjectManager::getInstance();
$customerSession = $objectManager->get(\Magento\Customer\Model\Session::class);

$isLoggedIn = $customerSession->isLoggedIn();
$initialMaxPrice = $block->getMaxPrice(); // Initial max price from block

$enabledFilters = $block->getEnabledFilters();

$showPriceFilter = in_array('price', $enabledFilters);
$showStockFilter = in_array('stock', $enabledFilters);
$shouldShowAnyFilter = $showPriceFilter || $showStockFilter;

// echo implode(',', $enabledFilters) . " All available";
?>

<?php if ($shouldShowAnyFilter): ?>
    <div class="filter-sidebar">
        <?php if ($showStockFilter): ?>
            <!-- Stock Filter -->
            <div class="filter-section">
                <input type="checkbox" id="stock-toggle" class="filter-toggle" checked>
                <label for="stock-toggle" class="filter-header">
                    <h3>Filter by Stock</h3>
                    <span class="toggle-icon"></span>
                </label>
                <div class="filter-content">
                    <label><input type="radio" name="stock_filter" value="all" checked> All</label><br>
                    <label><input type="radio" name="stock_filter" value="in"> In Stock</label><br>
                    <label><input type="radio" name="stock_filter" value="out"> Out of Stock</label>
                </div>
            </div>
        <?php endif; ?>
        <?php if ($showPriceFilter): ?>
            <!-- Price Filter -->
            <div class="filter-section" id="price-filter-sidebar">
                <input type="checkbox" id="price-toggle" class="filter-toggle" checked>
                <label for="price-toggle" class="filter-header">
                    <h3>Filter by Price</h3>
                    <span class="toggle-icon"></span>
                </label>
                <div class="filter-content">
                    <div class="range-slider-container">
                        <div class="range-slider">
                            <div class="range-track"></div>
                            <input type="range" id="price-min" min="0" max="<?= (int) $initialMaxPrice ?>" step="1" value="0" class="range-input">
                            <input type="range" id="price-max" min="0" max="<?= (int) $initialMaxPrice ?>" step="1" value="<?= (int) $initialMaxPrice ?>" class="range-input">
                        </div>
                    </div>
                    <div class="price-label">
                        <span id="price-range-label">£0 - £<?= (int) $initialMaxPrice ?></span>
                    </div>
                </div>
            </div>
        <?php endif; ?>
        <!-- Reset Button -->
        <div class="reset-section">
            <button id="reset-filters">Reset Filters</button>
        </div>
    </div>
<?php else: ?>
    <div class="no-filters-message" style="text-align:center; color:#999; margin: 30px auto;">
        <em>No filters available for your account.</em>
    </div>
    <input type="hidden" id="enabled-filters" value="<?= implode(',', $enabledFilters) ?>">

<?php endif; ?>
<style>
    /* General Styling */
    body,
    html {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        color: #2d2d2d;
        background: linear-gradient(135deg, #f5f7fa 0%, #e4e7eb 100%);
    }

    /* Sidebar Container */
    .filter-sidebar {
        max-width: 320px;
        margin: 30px auto;
        background: #ffffff;
        border-radius: 16px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
        padding: 24px;
        overflow: hidden;
    }

    /* Filter Section */
    .filter-section {
        margin-bottom: 16px;
        border-bottom: 1px solid #e5e7eb;
    }

    .filter-toggle {
        display: none;
    }

    .filter-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 16px;
        background: linear-gradient(90deg, #f8f9fa, #ffffff);
        border-radius: 10px;
        cursor: pointer;
        transition: background 0.3s, transform 0.2s;
    }

    .filter-header:hover {
        background: linear-gradient(90deg, #e9ecef, #f8f9fa);
        transform: translateY(-1px);
    }

    .filter-header h3 {
        font-size: 16px;
        font-weight: 600;
        color: #1f2937;
        margin: 0;
    }

    .toggle-icon {
        width: 16px;
        height: 16px;
        position: relative;
        transition: transform 0.3s;
    }

    .toggle-icon::before,
    .toggle-icon::after {
        content: '';
        position: absolute;
        background: #9d7a2f;
        height: 2px;
        width: 10px;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    .toggle-icon::before {
        transform: translate(-50%, -50%) rotate(90deg);
    }

    .filter-toggle:checked+.filter-header .toggle-icon::before {
        transform: translate(-50%, -50%) rotate(0deg);
    }

    .filter-content {
        padding: 16px;
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease, opacity 0.3s ease;
        opacity: 0;
    }

    .filter-toggle:checked~.filter-content {
        max-height: 200px;
        opacity: 1;
    }

    /* Stock Filter */
    .filter-content label {
        display: flex;
        align-items: center;
        margin: 1px 0;
        font-size: 14px;
        color: #9d7a2f;
        cursor: pointer;
        transition: color 0.2s;
    }

    .filter-content label:hover {
        color: #7a5f22;
    }

    .filter-content input[type="radio"] {
        accent-color: #f3b405;
        margin-right: 10px;
        width: 18px;
        height: 18px;
        cursor: pointer;
        transition: transform 0.2s;
    }

    .filter-content input[type="radio"]:hover {
        transform: scale(1.1);
    }

    /* Price Filter */
    #price-filter-sidebar .range-slider-container {
        margin: 20px 0;
    }

    #price-filter-sidebar .range-slider {
        position: relative;
        width: 100%;
        height: 8px;
    }

    #price-filter-sidebar .range-track {
        position: absolute;
        width: 100%;
        height: 8px;
        background: linear-gradient(90deg, #e5e7eb, #d1d5db);
        border-radius: 8px;
        z-index: 1;
    }

    #price-filter-sidebar .range-input {
        position: absolute;
        width: 100%;
        height: 8px;
        background: none;
        pointer-events: none;
        -webkit-appearance: none;
        z-index: 2;
        margin: 0;
    }

    #price-filter-sidebar .range-input::-webkit-slider-thumb {
        -webkit-appearance: none;
        width: 24px;
        height: 24px;
        background: #9d7a2f;
        border-radius: 50%;
        border: 3px solid #ffffff;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.15);
        cursor: pointer;
        pointer-events: auto;
        position: relative;
        z-index: 3;
        transition: transform 0.2s, background 0.2s;
    }

    #price-filter-sidebar .range-input::-webkit-slider-thumb:hover {
        transform: scale(1.15);
        background: #7a5f22;
    }

    #price-filter-sidebar .range-input::-moz-range-thumb {
        width: 24px;
        height: 24px;
        background: #9d7a2f;
        border-radius: 50%;
        border: 3px solid #ffffff;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.15);
        cursor: pointer;
        pointer-events: auto;
        position: relative;
        z-index: 3;
        transition: transform 0.2s, background 0.2s;
    }

    #price-filter-sidebar .range-input::-moz-range-thumb:hover {
        transform: scale(1.15);
        background: #7a5f22;
    }

    #price-filter-sidebar .range-input:focus {
        outline: none;
    }

    #price-filter-sidebar .price-label {
        text-align: center;
    }

    #price-filter-sidebar #price-range-label {
        font-size: 14px;
        font-weight: 500;
        color: #9d7a2f;
        background: #f5f5f5;
        padding: 8px 16px;
        border-radius: 10px;
        display: inline-block;
        box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.05);
    }

    /* Reset Button */
    .reset-section {
        text-align: center;
        margin-top: 20px;
    }

    #reset-filters {
        padding: 12px 24px !important;
        background: linear-gradient(90deg, #9d7a2f, #b38b36) !important;
        border: none !important;
        color: #ffffff !important;
        font-size: 14px;
        font-weight: 600;
        border-radius: 10px;
        cursor: pointer;
        transition: background 0.3s, transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
        width: 100%;
    }

    #reset-filters:hover {
        background: linear-gradient(90deg, #7a5f22, #9d7a2f) !important;
        transform: translateY(-2px);
        box-shadow: 0 5px 12px rgba(0, 0, 0, 0.15);
    }

    #reset-filters:active {
        transform: translateY(0);
        box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
    }
</style>